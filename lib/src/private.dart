/// pixel point
class PixelPoint {
  final int x;
  final int y;

  PixelPoint(this.x, this.y);
}

/// image size
class ImageSize {
  final int width;
  final int height;

  ImageSize(this.width, this.height);
}
